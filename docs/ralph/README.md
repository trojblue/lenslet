# Ralph Workspace Convention

- Default location for new Ralph loop workspaces: `docs/ralph/<YYYYMMDD_slug>/`.
- Typical files inside each workspace:
  - `progress.txt`
  - `ralph-plan.sh`
  - `ralph-plan-prompt.md`
- Keep one folder per plan/run context to avoid cross-run log mixing.
